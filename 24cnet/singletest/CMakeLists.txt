cmake_minimum_required(VERSION 2.8)
project(singletest)
find_package(OpenCV)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(singletest singletest.c)
target_link_libraries(singletest ${OpenCV_LIBS})
