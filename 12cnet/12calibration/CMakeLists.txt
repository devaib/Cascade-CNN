cmake_minimum_required(VERSION 2.8)
project(12calibration)
find_package(OpenCV)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(12calibration 12calibration.c)
target_link_libraries(12calibration ${OpenCV_LIBS})
