cmake_minimum_required(VERSION 2.8)
project(interpolations)
find_package(OpenCV)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(interpolations interpolations.cpp)
target_link_libraries(interpolations ${OpenCV_LIBS})
